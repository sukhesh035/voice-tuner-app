{
  "name": "backend-api",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/backend-api/src",
  "projectType": "application",
  "tags": ["scope:backend", "type:app"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/apps/backend-api"],
      "options": {
        "command": "node_modules/.bin/esbuild apps/backend-api/src/handlers/sessions.handler.ts apps/backend-api/src/handlers/users.handler.ts apps/backend-api/src/handlers/streaks.handler.ts apps/backend-api/src/handlers/classroom.handler.ts --bundle --minify --platform=node --target=node22 --format=cjs --external:@aws-sdk/client-dynamodb --external:@aws-sdk/lib-dynamodb --outdir=dist/apps/backend-api",
        "cwd": "{workspaceRoot}"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": { "lintFilePatterns": ["apps/backend-api/**/*.ts"] }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": { "jestConfig": "apps/backend-api/jest.config.ts" }
    },
    "deploy": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd apps/infra && npx cdk deploy --require-approval never",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
